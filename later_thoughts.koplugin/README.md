# Later Thoughts for KOReader

Minimal, book-independent notes for KOReader.

This folder is the installable plugin. Copy the entire `later_thoughts.koplugin` folder into KOReader's `plugins` directory.

Existing users of the development builds: the plugin intentionally continues using the same notes storage folder so your notes remain available after upgrading.
